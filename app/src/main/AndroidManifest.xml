<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="peps.company.algerie.com" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="peps.company.algerie.com.LoginActivity"
            android:label="PEPS"
            android:theme="@style/AppTheme.Dark"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="peps.company.algerie.com.MainActivity" android:theme="@style/AppTheme.Dark" />
        <activity android:name="peps.company.algerie.com.SignupActivity" android:theme="@style/AppTheme.Dark" />
    </application>

</manifest>
